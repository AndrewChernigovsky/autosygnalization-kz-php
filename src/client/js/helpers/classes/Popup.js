export default class Popup {
  // constructor(context) {
  //   this.elem = this.bind(context)
  // }

  initPopup(elem, id, count) {
    this.elem = elem;
    this.id = id;
    console.log(this.elem, id, count);
  }
}